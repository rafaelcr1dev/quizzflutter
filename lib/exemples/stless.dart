import 'package:flutter/material.dart';

class ExempleApp extends StatelessWidget {
  const ExempleApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
